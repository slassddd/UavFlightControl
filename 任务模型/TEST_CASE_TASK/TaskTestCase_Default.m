function TestCase_Task = TaskTestCase_Default()
% 初始化TestCase结构体
%% Cmd76
% 时间触发命令
TestCase_Task.GroundStation.MavlinkCmd76.time = [0];
TestCase_Task.GroundStation.MavlinkCmd76.data = [...
    ENUM_Mav76.Mav76_None];
% 条件触发命令——暂停(按顺序判断执行)
TestCase_Task.GroundStation.doPause(10) = Simulink.Bus.createMATLABStruct('BUS_TESTCASE_PauseContinueParam');
for i = 1:length(TestCase_Task.GroundStation.doPause)
    TestCase_Task.GroundStation.doPause(i) = doPauseForTestCase(TestCase_Task.GroundStation.doPause(1),...
    'taskMode',ENUM_FlightTaskMode.NoneFlightTaskMode,...
    'pointNumber',0,...
    'delay',0,...
    'duration',0,...
    'bus',TestCase_Task.GroundStation.doPause(1));
end